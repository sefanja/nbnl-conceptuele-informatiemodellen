title: Stelsel van energieregisters
theme: just-the-docs
baseurl: /nbnl-conceptuele-informatiemodellen

collections:
  registers:
    output: true

just_the_docs:
  collections:
    registers:
      name: Registers

defaults:
  - scope:
      path: ""
    values:
      layout: default

color_scheme: nbnl
logo: "/assets/images/logo.svg"
fav_icon: "/assets/images/favicon.png"

callouts:
  warning:
    title: Verplaatst
    color: red
