title: NBNL Conceptuele Informatiemodellen
theme: just-the-docs
baseurl: /nbnl-conceptuele-informatiemodellen

collections:
  modellen:
    output: true

just_the_docs:
  collections:
    modellen:
      name: Modellen

include:
  - "**/*.svg"

defaults:
  - scope:
      path: ""
      type: modellen
    values:
      layout: default
